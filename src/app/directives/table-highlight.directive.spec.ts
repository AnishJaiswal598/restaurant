import { TableHighlightDirective } from './table-highlight.directive';

describe('TableHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
