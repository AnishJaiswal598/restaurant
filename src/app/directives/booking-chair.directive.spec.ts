import { BookingChairDirective } from './booking-chair.directive';

describe('BookingChairDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingChairDirective();
    expect(directive).toBeTruthy();
  });
});
