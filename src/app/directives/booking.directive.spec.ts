import { BookingDirective } from './booking.directive';

describe('BookingDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingDirective();
    expect(directive).toBeTruthy();
  });
});
